target/
*.log
.DS_Store
*.pdb
*.dll
*.lib
*.exp
*.d

# 环境变量（可能含密钥）
.env
.env.*
!.env.example

# TRAE workspace files
.trae/

# 运行时数据（evorule-server 数据库等）
data/

# 内部设计文档（不跟踪 git，仅供本地参考）
_design_docs/

# 内部验证报告（不跟踪 git，仅供本地参考）
文档/

# 本地工作/临时目录
.build/
_tmp_*

# demo 资产（demo.ps1/demo.sh 下载的 server 包与运行日志）
.demo/
# file_write 工具沙箱目录（demo 业务产物落盘处）
workspace/


# 本地运行配置(含端点选择与本地路径,不随仓分发,密钥走 ENV 引用)
evo-agent.toml

# 运维脚本本机实例配置(含本机真实路径,不随仓分发)
scripts/ops/ops.local.json

# 工作台前端(web/):依赖与构建产物不跟踪,源码正常入库
web/node_modules/
web/dist/

# 本地测试输出（cargo test 重定向产物）
test_all_output.txt

# Python E2E 脚本字节码缓存
__pycache__/
